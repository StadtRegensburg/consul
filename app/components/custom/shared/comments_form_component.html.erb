<%= render Proposals::CommentsFormComponent.new(record) if record.class.name == 'Proposal' %>
<%= render Debates::CommentsFormComponent.new(record) if record.class.name == 'Debate' %>
<%= render Polls::CommentsFormComponent.new(record) if record.class.name == 'Poll' %>
<%= render Pages::CommentsFormComponent.new(record) if record.class.name == 'Projekt' %>
<%= render ProjektQuestions::CommentsFormComponent.new(record) if record.class.name == 'ProjektQuestion' %>
<%= render DeficiencyReports::CommentsFormComponent.new(record) if record.class.name == 'DeficiencyReport' %>
<%= render Topics::CommentsFormComponent.new(record) if record.class.name == 'Topic' %>
