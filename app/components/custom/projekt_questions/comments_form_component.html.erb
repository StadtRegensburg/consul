<% if @projekt.present? &&
      @projekt_question.comments_allowed?(current_user) %>

  <%= render "comments/form", { commentable: @projekt_question, parent_id: nil } %>
<% else %>

  <%= render "shared/login_to_comment" %>
<% end %>
