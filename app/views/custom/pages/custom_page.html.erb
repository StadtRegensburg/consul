<%= content_for :body_class, "custom-page" %>
<% provide :title do %><%= @custom_page.title %><% end %>

<main>
  <div class="row">
    <div class="column">
      <% if @custom_page.projekt.present? %>
        <%= render partial: 'breadcrumbs', locals: { current_page: @custom_page } %>
        <div class='breadcrumbs-item'>
          <%= @custom_page.projekt.name %>
        </div >
      <% end %>

      <h1><%= @custom_page.title %></h1>
      <% if @custom_page.subtitle.present? %>
        <h2><%= @custom_page.subtitle %></h2>
      <% end %>

      <% if @custom_page.projekt.present? %>
        <div class="row">
          <div class="small-12 medium-9 column">
            <%= render 'custom_page_content' %>
          </div>

          <div class="small-12 medium-3 column">
            <%= render partial: 'pages/sidebar/projekts_sidebar', locals: { projekt: @custom_page.projekt } %>
          </div>
        </div>

      <% else %>
        <%= render 'custom_page_content' %>
      <% end %>

    </div>
  </div>

  <% if @custom_page.projekt.present? && projekt_feature?(@custom_page.projekt, 'show_projekt_footer') %>
    <div class="additional-content projekt-footer">
      <div class="filter-subnav">
        <div class="row">
          <div class="small-12 column">
            <%= render "custom/pages/projekt_footer/filter_subnav" %>
          </div>
        </div>
      </div>
    </div>

    <div class="tabs-content" data-tabs-content="projekts_tabs">

      <%= render "custom/pages/projekt_footer/activities" %>

      <% if projekt_feature?(@custom_page.projekt, 'show_comments_in_projekt_footer') %>
        <div class="tabs-panel" id="tab-comments">
          <%= render "custom/pages/projekt_footer/comments" %>
        </div>
      <% end %>

      <% if projekt_feature?(@custom_page.projekt, 'show_notifications_in_projekt_footer') %>
        <%= render "custom/pages/projekt_footer/notifications" %>
      <% end %>

      <% if projekt_feature?(@custom_page.projekt, 'show_milestones_in_projekt_footer') %>
        <%= render "milestones/milestones", milestoneable: @custom_page.projekt %>
      <% end %>

      <% if projekt_feature?(@custom_page.projekt, 'show_newsfeed_in_projekt_footer') &&
            ProjektSetting.find_by(projekt_id: 1, key: "projekt_newsfeed.id").value.present? &&
            ProjektSetting.find_by(projekt_id: 1, key: "projekt_newsfeed.type").value.present? %>
        <%= render "custom/pages/projekt_footer/newsfeed" %>
      <% end %>
    </div>
  <% end %>
</main>
