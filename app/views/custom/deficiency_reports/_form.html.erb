<%= render "shared/globalize_locales", resource: @deficiency_report %>

<%= translatable_form_for(@deficiency_report, url: form_url) do |f| %>

  <%= render "shared/errors", resource: @deficiency_report %>

  <div class="row column">
    <%= f.translatable_fields do |translations_form| %>
      <div class="small-12 column">
        <%= translations_form.text_field :title %>
      </div>

      <div class="small-12 column">
        <%= translations_form.text_area :description,
              class: "html-area #{ck_editor_class(current_user)}" %>
      </div>
    <% end %>

    <%= f.invisible_captcha :subtitle %>

    <div class="small-12 column margin-top">
      <%= render partial: "shared/projekt_selector/projekt_selector", locals: { f: f, model: @deficiency_report }  %>
    </div>

    <div class="small-12 column <%= show_map_in_form?(@selected_projekt) %>" id='map-for-new-proposal'>

      <%= render "map_locations/form_fields",
                 form:     f,
                 map_location: @deficiency_report.map_location || @selected_projekt&.map_location || MapLocation.new,
                 label:    t("proposals.form.map_location"),
                 help:     t("proposals.form.map_location_instructions"),
                 remove_marker_label: t("proposals.form.map_remove_marker"),
                 parent_class: "proposal",
                 i18n_namespace: "proposals" %>

    </div>

    <%if extended_feature?("modulewide.enable_categories") %>
      <div class="small-12 column margin-bottom">
        <%if @categories.any? %>
          <div id="category_tags" class="tags">
            <%= f.label :category_tag_list, t("proposals.form.tag_category_label") %>
            <% @categories.each do |tag| %>
              <a class="js-add-tag-link category-tag"><%= tag.name %></a>
            <% end %>
          </div>
        <%end%>
      </div>
      <%=f.hidden_field :tag_list_predefined, value: @deficiency_report.tags.reject{|t| !['category'].include?(t.kind)}.map(&:name).join(","), class: "js-tag-list list-tags predefined"%>
    <%end%>

    <%if extended_feature?("modulewide.enable_custom_tags") %>
      <div class="small-12 column margin-bottom">
        <%= f.text_field :tag_list_custom, value: @deficiency_report.tags.where(kind: nil).map{|c| c.name}.join(","),
          label: t("custom.proposals.form.tags_custom"),
          placeholder: t("custom.proposals.form.tags_placeholder"),
          data: { js_url: suggest_tags_path },
          class: "tag-autocomplete list-categories" %>
      </div>
    <%end%>

    <%= render SDG::RelatedListSelectorComponent.new(f) %>

    <div class="small-12 column">
      <% if @deficiency_report.new_record? %>
        <%= f.check_box :terms_of_service,
          title: t("form.accept_terms_title"),
          label: t("form.accept_terms",
                   policy: link_to(t("form.policy"), "/privacy", target: "blank"),
                   conditions: link_to(t("form.conditions"), "/conditions", target: "blank")
                  ) %>
      <% end %>
    </div>

    <div class="actions small-12 column">
      <%= f.submit(class: "button", value: t("proposals.#{action_name}.form.submit_button")) %>
    </div>
  </div>
<% end %>
