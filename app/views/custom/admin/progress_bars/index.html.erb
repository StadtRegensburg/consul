<% provide :title do %>
  <%= "#{t("admin.header.title")} - #{t("admin.progress_bars.index.title")}" %>
<% end %>

<%= back_link_to edit_admin_projekt_path(@progressable) + "#tab-projekt-milestones" %>

<div class="clear"></div>

<%= render "admin/progress_bars/progress_bars", progressable: @progressable %>
