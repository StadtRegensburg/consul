<h2><%= t('custom.admin.projekts.overview_page_tab.title') %></h2>
<table>
  <thead>
    <tr>
      <th><%= t("admin.settings.setting") %></th>
      <th><%= t("admin.settings.index.features.enabled") %></th>
    </tr>
  </thead>
  <tbody>
    <% @projekts_overview_page_navigation_settings.each do |feature| %>
      <tr>
        <td>
          <strong id="<%= dom_id(feature, :title) %>"><%= t("custom.settings.#{feature.key}") %></strong>
          <br>
          <span class="small">
            <%= t("custom.settings.#{feature.key}_description", default: t("admin.settings.no_description")) %>
          </span>
        </td>

        <td>
          <%= render Admin::Settings::FeaturedSettingsFormComponent.new(feature, tab: '#tab-projekts-overview-page') %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<h2><%= t('custom.admin.projekts.overview_page_tab.title_footer') %></h2>
<table>
  <thead>
    <tr>
      <th><%= t("admin.settings.setting") %></th>
      <th><%= t("admin.settings.index.features.enabled") %></th>
    </tr>
  </thead>
  <tbody>
    <% @projekts_overview_page_navigation_settings.each do |feature| %>
      <tr>
        <td>
          <strong id="<%= dom_id(feature, :title) %>"><%= t("custom.settings.#{feature.key}") %></strong>
          <br>
          <span class="small">
            <%= t("custom.settings.#{feature.key}_description", default: t("admin.settings.no_description")) %>
          </span>
        </td>

        <td>
          <%= render Admin::Settings::FeaturedSettingsFormComponent.new(feature, tab: '#tab-projekts-overview-page') %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<h2 class="margin-top-25"><%= t("custom.admin.projekts.edit.projekt_phases_tab.phases_block_title") %></h2>

<table class='edit-projekt margin-top'>
  <tbody>

    <%= render partial: 'custom/admin/projekts/edit/projekt_phases/edit_projekt_phase', locals: {
      projekt: @overview_page_special_projekt,
      projekt_phase: @overview_page_special_projekt.comment_phase,
      phase_name: 'comment_phase'
    } %>

    <%= render partial: 'custom/admin/projekts/edit/projekt_phases/edit_projekt_phase', locals: {
      projekt: @overview_page_special_projekt,
      projekt_phase: @overview_page_special_projekt.debate_phase,
      phase_name: 'debate_phase'
    } %>

    <%= render partial: 'custom/admin/projekts/edit/projekt_phases/edit_projekt_phase', locals: {
      projekt: @overview_page_special_projekt,
      projekt_phase: @overview_page_special_projekt.proposal_phase,
      phase_name: 'proposal_phase'
    } %>

    <%= render partial: 'custom/admin/projekts/edit/projekt_phases/edit_projekt_phase', locals: {
      projekt: @overview_page_special_projekt,
      projekt_phase: @overview_page_special_projekt.voting_phase,
      phase_name: 'voting_phase'
    } %>
  </tbody>
</table>
