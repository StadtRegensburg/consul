$("#<%= dom_id(@proposal) %>_votes").html("<%= j render("proposals/votes", proposal: @proposal, vote_url: vote_proposal_path(@proposal, value: "yes")) %>");

App.Followable.update("<%= dom_id(@follow.followable) %>",
                      "<%= j render("follows/follow_button", follow: @follow) %>",
                      "<%= j render("layouts/flash") %>")
